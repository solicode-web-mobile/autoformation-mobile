---
title: 6. Configuration du poste Windows pour le développement
layout: home
nav_order: 6
parent: 7.1. Logique binaire & matériel PC
permalink: /binaire-materiel/configuration-poste-dev/
code: 7.1.6
competence: C7
autoformation: "C7.1"
ua: "7.1.U3"
duree_h: 1.5
objectif: "Configurer un poste Windows propre et fonctionnel pour le développement web (PHP, Laravel, Git, VS Code)."
notions_nouvelles: ["Fichiers cachés", "Extensions de fichiers", "Chemin (PATH)", "Terminal", "Pare-feu", "Antivirus", "Arborescence de travail", "Hosts file"]
fil_rouge: "Préparer le poste de développement pour exécuter et déployer le blog Laravel localement."
livrable_chapitre: "Poste Windows configuré (affichage, PATH, réseau local), captures de validation et mini script d’ouverture du projet."
alimentation_prototype: "Projet Laravel exécutable localement avec un environnement Windows stable et organisé."
alimentation_miniprojet: "Configuration reproductible documentée pour le déploiement sur d’autres postes."
---

# 🧩 Chapitre 7.1.6 — Configuration du poste Windows pour le développement

## 🎯 Objectif pédagogique

Rendre un poste **Windows** prêt pour le **développement web professionnel** :  
fichiers visibles, terminal configuré, PATH fonctionnel, ports disponibles et environnement sécurisé.  
L’objectif est de **travailler efficacement** et d’éviter les erreurs dues à une mauvaise configuration système.

---

## 📒 Glossaire minute

| Terme | Définition simple |
|--------|-------------------|
| **Fichiers cachés** | Fichiers invisibles par défaut (ex. `.env`, `.gitignore`). |
| **Extensions** | Partie finale d’un nom de fichier (`.php`, `.json`, `.env`). |
| **PATH** | Liste de chemins où Windows cherche les programmes exécutables. |
| **Terminal / PowerShell** | Interface de commande pour lancer et diagnostiquer des programmes. |
| **Pare-feu** | Outil Windows qui bloque ou autorise le trafic réseau. |
| **Hosts file** | Fichier local permettant d’associer un nom de domaine à une adresse IP. |

---

## 🧠 Étape 1 — Afficher les fichiers cachés et extensions

> Windows cache souvent les informations dont un développeur a besoin.

### 📍 À faire :
1. Ouvre l’**Explorateur de fichiers** → onglet **Affichage → Options → Modifier les options de dossier**.  
2. Onglet **Affichage** :
   - ✅ Afficher les **fichiers, dossiers et lecteurs cachés**.  
   - ✅ Décocher “Masquer les extensions des fichiers connus”.  
   - ✅ Décocher “Masquer les fichiers protégés du système d’exploitation”.  
3. Applique les changements.

🧩 **Vérifie :**
- Tu vois `.env`, `.gitignore`, `.vscode/`, etc. dans ton projet.  
- Les fichiers affichent bien leurs extensions (`index.php`, `config.json`...).

---

## 🧱 Étape 2 — Organiser ton arborescence de travail

### Structure recommandée :
```

C:
├── sites\         → tous les projets web
│    ├── blog
│    ├── api-demo
│    └── portfolio
├── tools\         → outils manuels (php, composer, node, git)
├── data\          → backups, exports, dumps SQL
└── temp\          → tests, fichiers temporaires

```

### Bonnes pratiques :
- Pas de projets sur le **Bureau** ou dans **Documents**.  
- Noms de dossiers en **minuscules**, **sans accents ni espaces**.  
- Les chemins doivent être simples : `C:\sites\blog\`.

🧠 Cela facilite la configuration du serveur Apache ou des scripts batch.

---

## ⚙️ Étape 3 — Vérifier le PATH et les commandes globales

### Objectif :
Pouvoir lancer PHP, Composer, Git et Node depuis n’importe quel dossier.

Ouvre **PowerShell** et tape :

```bash
php -v
composer -V
git --version
node -v
npm -v
```

Si tu obtiens une erreur “commande non reconnue” :

1. Recherche “Variables d’environnement”.
2. Modifie la variable **PATH** et ajoute (si besoin) :

   ```
   C:\xampp\php
   C:\ProgramData\ComposerSetup\bin
   C:\Program Files\Git\bin
   C:\Program Files\nodejs
   ```
3. Redémarre ton terminal.

---

## 💻 Étape 4 — Configurer le terminal de développement

### Recommandé : **Windows Terminal** (Microsoft Store)

* Permet d’utiliser **PowerShell**, **CMD** et **Git Bash**.
* Personnalise la couleur, la police et le dossier d’ouverture par défaut : `C:\sites`.

### Commandes utiles :

```bash
cd C:\sites\blog
dir
php artisan serve
ping localhost
netstat -ano | find "80"
```

> 💡 Astuce : crée un raccourci “Ouvrir dans le terminal ici” dans le menu contextuel.

---

## 🌐 Étape 5 — Vérifier la configuration réseau locale

### Tester les ports :

```bash
netstat -ano | find "80"
```

```powershell
netstat -ano | Select-String "80"
```


Si le port 80 est déjà pris (par Skype ou IIS) :

* change le port d’Apache dans `C:\xampp\apache\conf\httpd.conf`
  → ligne `Listen 8080`

### Fichier hosts :

```
C:\Windows\System32\drivers\etc\hosts
```

Ajouter une ligne :

```
127.0.0.1   monblog.local
```

> Cela permet d’accéder à ton projet via `http://monblog.local`.

---


## 🧩 Étape 6 — Vérification et mini-script de démarrage

Crée un fichier `start_blog.bat` dans ton projet :

```bat
@echo off
cd /d C:\sites\blog
start php artisan serve
start http://localhost:8000
echo Blog Laravel lancé !
pause
```

🧪 **Résultat attendu :**

* Le terminal s’ouvre dans ton projet.
* Le serveur démarre automatiquement.
* Le navigateur affiche le site.

---


## 🧾 Livrable attendu

* 3 captures d’écran :

  * Explorateur (fichiers cachés + extensions visibles)
  * Terminal avec `php -v` et `composer -V`
  * Site local fonctionnel (`http://monblog.local` ou `localhost:8000`)
* Script batch `start_blog.bat` dans ton projet.

---

## ✅ Résumé

Tu sais maintenant :

* Rendre Windows **lisible et propre** pour coder efficacement.
* Vérifier et corriger le **PATH**.
* Travailler dans un **terminal configuré**.
* Tester et sécuriser ton **environnement local**.

