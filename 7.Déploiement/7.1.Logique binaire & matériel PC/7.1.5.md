---
title: 5. Boot & services
layout: home
nav_order: 5
parent: 7.1. Logique binaire & matériel PC
permalink: /binaire-matériel/boot-services/
code: 7.1.5
competence: C7
autoformation: "C7.1"
ua: "7.1.U3"
duree_h: 2
objectif: "Découvrir comment un ordinateur démarre, gère ses processus et active les services nécessaires au déploiement d’une application."
notions_nouvelles: ["UEFI/BIOS", "POST", "MBR/GPT", "Bootloader", "Kernel", "Init/PID1", "Service/Daemon", "Target (systemd)"]
fil_rouge: "Démarrage fiable des services (web/DB) pour environnements de dev et démo"
livrable_chapitre: "Table boot (firmware/MBR-GPT/ordre), commandes status/enable/logs, mini-incident résolu"
alimentation_prototype: "Services essentiels démarrés et vérifiables pour la démo N2"
alimentation_miniprojet: "Pratiques Ops minimales (services/logs/boot) pour serveurs N3"
---


# ⚙️ 7.1.5 — Démarrage (BIOS/UEFI), processus & services


## 🎯 Objectif
Comprendre comment un **ordinateur démarre**, comment le **système d’exploitation** se charge,  
et comment les **services** (comme Apache ou MySQL) s’exécutent automatiquement au lancement.  

---

## 🧠 Introduction théorique

Avant de déployer une application web, il faut que ton ordinateur (ou ton serveur) **sache démarrer, charger son système, et lancer les bons services**.  
Ces étapes garantissent que ton application fonctionnera même après un redémarrage ou une coupure.  

Tu vas apprendre :
- le rôle du **BIOS/UEFI** dans le démarrage,  
- ce qu’est un **processus** et comment le visualiser,  
- comment **lancer et vérifier un service** (Apache, MySQL, SSH),  
- et comment **contrôler l’état d’un service** grâce à quelques commandes simples.

---

## 📘 Étape 1 — Le démarrage de la machine : du BIOS au système

Quand tu allumes ton ordinateur, il suit une séquence appelée **POST (Power-On Self Test)** :
1. Le **BIOS/UEFI** détecte et initialise le matériel (RAM, disques, carte réseau).  
2. Il cherche un **périphérique de démarrage** (disque, clé USB, etc.).  
3. Il charge le **bootloader** (programme qui lance le système d’exploitation).  
4. Le **système d’exploitation (OS)** démarre et prend le relais.

💡 Sur un PC moderne, le **UEFI** a remplacé le BIOS classique.  
Il est plus rapide, plus sécurisé et permet le démarrage sur des disques plus grands.

🧪 **À faire :**
- Redémarre ton PC et repère la touche pour accéder au BIOS/UEFI (`F2`, `DEL`, `ESC` selon la marque).  
- Observe les options de **Boot Order** et **Information système**.

---

## 🧩 Étape 2 — Comprendre les processus en cours

Un **processus** est un programme en cours d’exécution (comme ton navigateur ou le serveur Apache).  

### 🔹 Sous Linux :
Ouvre le terminal et tape :
```bash
ps aux | head
````

Cette commande affiche la liste des processus actifs.

Pour une vue plus claire :

```bash
top
```

ou, si installé :

```bash
htop
```

### 🔹 Sous Windows :

Appuie sur **Ctrl + Maj + Échap** → ouvre le **Gestionnaire des tâches** → onglet **Processus**.

🧪 **À faire :**

> Observe quels processus utilisent le plus de CPU ou de mémoire.

---

## 🧩 Étape 3 — Découvrir et contrôler les services

Les **services** sont des programmes qui tournent en arrière-plan, souvent dès le démarrage.
Exemples : Apache (serveur web), MySQL (base de données), SSH (accès distant).

### 🔹 Sous Linux :

```bash
systemctl status apache2
```

```bash
systemctl start apache2
```

```bash
systemctl enable apache2
```

> Ces commandes permettent de **vérifier**, **lancer** et **activer au démarrage** un service.

### 🔹 Sous Windows :

Ouvre **Exécuter → services.msc**
Cherche **Apache**, **MySQL**, ou **wampapache**.
Tu peux démarrer/arrêter un service via clic droit → *Démarrer / Arrêter*.

---

## 🧩 Étape 4 — Vérifier les ports utilisés

Chaque service écoute sur un **port réseau** :

* **80** → HTTP (Apache)
* **443** → HTTPS
* **3306** → MySQL
* **22** → SSH

### 🔹 Vérifier les ports ouverts

```bash
sudo netstat -tuln | grep 80
```

ou

```bash
sudo ss -lntp
```

> Cela affiche les processus actifs liés aux ports (utile pour vérifier qu’un serveur web fonctionne).

---

## 🧩 Étape 5 — Résumé pratique

| Élément   | Outil                    | Commande / Action                             |
| --------- | ------------------------ | --------------------------------------------- |
| BIOS/UEFI | BIOS Setup               | Vérifier l’ordre de démarrage                 |
| Processus | Terminal / Task Manager  | `top`, `ps aux`, “Gestionnaire des tâches”    |
| Services  | systemctl / services.msc | `systemctl status apache2`, `net start mysql` |
| Ports     | netstat / ss             | `netstat -tuln`                               |

---

## 💡 Erreurs fréquentes

| Problème                  | Cause probable               | Solution                           |
| ------------------------- | ---------------------------- | ---------------------------------- |
| Apache ne démarre pas     | Port 80 déjà occupé          | Vérifie avec `netstat -tuln`       |
| MySQL plante au démarrage | Mauvais fichier config       | Vérifie `my.cnf` ou `my.ini`       |
| Accès refusé              | Pas de privilèges admin/root | Utilise `sudo` ou exécute en admin |

---

## ✅ Résumé

Tu sais maintenant :

* Ce qu’est le **BIOS/UEFI** et son rôle dans le démarrage.
* Ce qu’est un **processus** et comment le visualiser.
* Comment **démarrer et vérifier un service** (Apache/MySQL).
* Comment **vérifier les ports réseau** utilisés par ces services.

---
